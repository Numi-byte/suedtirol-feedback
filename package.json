{
  "name": "suedtirol-feedback",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:client": "npm run dev --prefix client",
    "dev:protected": "npm run dev --prefix protected",
    "build": "npm run build --prefix client && npm run build --prefix protected",
    "lint": "npm run lint --prefix client && npm run lint --prefix protected",
    "typecheck": "npm run typecheck --prefix client && npm run typecheck --prefix protected"
  }
}
